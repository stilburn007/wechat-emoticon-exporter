# Core dependency: AES for emoticon decryption.
pycryptodome>=3.18

# Optional: bundles a static ffmpeg used to transcode .wxgf (HEVC) stickers to GIF.
# Install with:  pip install "wechat-emoticon-exporter[wxgf]"
imageio-ffmpeg>=0.4.9
